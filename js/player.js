window._restoreMusicTime =function (){var audio =do cument.getElementById('bg-audio');if (!audio)return ;var t =parseFloat(sessionStorage.getItem('music_time')||'0');if (t >0 &&isFinite(t))audio.currentTime =t;};var SONGS =[{src:'audio/cry.mp3',cover:'media/music-cover/cry.jpg',title:'cry.mp3'},{src:'audio/the-night-we-met.mp3',cover:'media/music-cover/the-night-we-met.jpg',title:'the-night-we-met.mp3'}];
var _idx =sessionStorage.getItem('song_index');if (_idx ===null){_idx =Math.floor(Math.random()*SONGS.length);sessionStorage.setItem('song_index',_idx);}else {_idx =parseInt(_idx,10);}var audio =do cument.getElementById('bg-audio');var playPauseBtn =do cument.getElementById('play-pause-btn');var prevBtn =do cument.getElementById('prev-btn');var nextBtn =do cument.getElementById('next-btn');var muteBtn =do cument.getElementById('mute-btn');var volumeBar =do cument.getElementById('volume-bar');var progressContainer =do cument.getElementById('progress-container');var progressBar =do cument.getElementById('music-progress');var timeEl =do cument.getElementById('music-time');
function getCoverEl(){return do cument.getElementById('music-cover')||do cument.querySelector('.music-player-card img');}function getTitleEl(){return do cument.querySelector('.song-title');}function loadSong(idx,autoplay){_idx =((idx %SONGS.length)+SONGS.length)%SONGS.length;sessionStorage.setItem('song_index',_idx);var song =SONGS[_idx];
var srcEl =audio.querySelector('source');if (!srcEl){srcEl =do cument.createElement('source');srcEl.type ='audio/mpeg';audio.appendChild(srcEl);}srcEl.src =song.src;audio.load();
var coverEl =getCoverEl();if (coverEl){coverEl.src =song.cover;coverEl.style.display ='';var fallback =do cument.getElementById('cover-fallback');if (fallback)fallback.style.display ='none';}var titleEl =getTitleEl();if (titleEl)titleEl.textContent =song.title;
if (progressBar)progressBar.style.width ='0%';if (timeEl)timeEl.textContent ='-0:00';if (autoplay)audio.play().catch (function (){});}loadSong(_idx,false);
if (prevBtn)prevBtn.addEventListener('click',function (){loadSong(_idx -1,!audio.paused);});if (nextBtn)nextBtn.addEventListener('click',function (){loadSong(_idx +1,!audio.paused);});audio.addEventListener('ended',function (){loadSong(_idx +1,true);});
function _updateMuteIcon(){if (!muteBtn)return ;var muted =audio.muted ||audio.volume ===0;muteBtn.className =muted?'fas fa-volume-mute control-icon':(audio.volume <0.5?'fas fa-volume-do wn control-icon':'fas fa-volume-up control-icon');}var _savedVol =parseFloat(localStorage.getItem('music_volume')||'1');audio.volume =_savedVol;if (volumeBar)volumeBar.value =_savedVol;_updateMuteIcon();if (volumeBar){volumeBar.addEventListener('in put',function (e){var vol =parseFloat(e.target.value);audio.volume =vol;audio.muted =vol ===0;localStorage.setItem('music_volume',vol);_updateMuteIcon();});}if (muteBtn){muteBtn.addEventListener('click',function (){audio.muted =!audio.muted;if (!audio.muted &&audio.volume ===0){audio.volume =0.5;if (volumeBar)volumeBar.value =0.5;localStorage.setItem('music_volume',0.5);}_updateMuteIcon();});}if (playPauseBtn){playPauseBtn.addEventListener('click',function (){if (audio.paused){audio.play().catch (function (){});}else {audio.pause();}});}audio.addEventListener('pause',function (){if (playPauseBtn)playPauseBtn.classList.replace('fa-pause','fa-play');});audio.addEventListener('play',function (){if (playPauseBtn)playPauseBtn.classList.replace('fa-play','fa-pause');});
function fmtTime(s){if (!isFinite(s)||isNaN(s))return '0:00';var m =Math.floor(s /60),sec =Math.floor(s %60);return m +':' +(sec <10?'0':'')+sec;}var _seeking =false;audio.addEventListener('timeupdate',function (){if (_seeking ||!audio.duration ||!isFinite(audio.duration))return ;var pct =(audio.currentTime /audio.duration)*100;if (progressBar)progressBar.style.width =pct +'%';sessionStorage.setItem('music_time',audio.currentTime);if (timeEl)timeEl.textContent ='-' +fmtTime(audio.duration -audio.currentTime);});function seekFromEvent(e){if (!audio.duration ||!isFinite(audio.duration)||!progressContainer)return ;var rect =progressContainer.getBoundingClientRect();var clientX =e.touches?e.touches[0].clientX:e.clientX;var ratio =Math.max(0,Math.min(1,(clientX -rect.left)/rect.width));audio.currentTime =ratio *audio.duration;if (progressBar)progressBar.style.width =(ratio *100)+'%';if (timeEl)timeEl.textContent ='-' +fmtTime(audio.duration -audio.currentTime);}if (progressContainer){progressContainer.addEventListener('click',seekFromEvent);progressContainer.addEventListener('mousedown',function (e){_seeking =true;seekFromEvent(e);function onMove(ev){seekFromEvent(ev);}function onUp(){_seeking =false;window.removeEventListener('mousemove',onMove);window.removeEventListener('mouseup',onUp);}window.addEventListener('mousemove',onMove);window.addEventListener('mouseup',onUp);});progressContainer.addEventListener('touchstart',function (e){_seeking =true;seekFromEvent(e);},{passive:true});progressContainer.addEventListener('touchmove',function (e){seekFromEvent(e);},{passive:true});progressContainer.addEventListener('touchend',function (){_seeking =false;});}window._restoreMusicTime =function (){var t =parseFloat(sessionStorage.getItem('music_time')||'0');if (t >0 &&isFinite(t)&&audio)audio.currentTime =t;};